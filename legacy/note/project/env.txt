env.txt

#pc
'aux where <aux command>': Print the path of aux command shell script
git back 관련 command 가 불필요한 git 에 대해서는 짧게 출력하여 가독성을 높여보자
structure 의 image 를 upload 하기 혹은 table 로 만들어서 이미지화 하기
vimsession 은 읽을 파일을 절대 경로로 저장하는 단점이 있다
- 유동적으로 session 을 실행할 directory 를 설정할 수는 없을까?
setenv 가 basecamp 를 입력받아서 패키지를 구성하는 기능
전반적인 초기 과정을 추적해보자, ssh key 를 적용하기 위해서 필요한 것들 -> 어디에 적용할지 사이트 주소
set env 를 더을 세분화해서 모듈화?

#finish
aux backcheck 가 directory 가 아닌, file 에 대해서도 동작하는 버그
- ex> .DS_store
